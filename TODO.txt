1. Add structs (data classes)
2. Change `` to regex literal rather than a String literal
3. Add other Core models including Grid, Version and others that make sense.
4. Add reflection and extensions for use when hostLanguage is set to true.
5. Work on core library. Keep it light for now.

--

v3: Custom DSL blocks like:
    let block = lang KD {
        // ...
    }

